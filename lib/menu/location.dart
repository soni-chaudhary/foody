import 'package:flutter/material.dart';

class LocationPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
      body: Center(child: Text("LocationPages")),
    );
  }
}
